Module Module5
    Sub Main()
        'Variables
        Dim precio_inicial As Byte
        Dim rebaja As Byte
        Dim precio_final As Byte
        precio_inicial = 50
        rebaja = 0.15
        precio_final = 50 * 0.15
        Console.WriteLine("El precio final es de " & precio_final)
        Console.ReadLine()
    End Sub
End Module
