Module Module1

    Sub Main()
        'Zona de variables
        Dim base, altura, area As Single
        'Entrada
        base = 9
        altura = 4
        'Proceso
        area = (base * altura) / 2
        'Salida
        Console.WriteLine("El area es " & area)
        Console.ReadLine()
    End Sub

End Module
